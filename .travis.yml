language: node_js
node_js:
  - "4"

install:
  - npm install
  - npm install pixi-gl-core -save
  - npm install bit-twiddle

cache:
  directories:
    - node_modules

script:
  - gulp build
  - gulp doc
